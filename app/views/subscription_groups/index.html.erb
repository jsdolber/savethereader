<h1>Listing subscription_groups</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subscription_groups.each do |subscription_group| %>
  <tr>
    <td><%= link_to 'Show', subscription_group %></td>
    <td><%= link_to 'Edit', edit_subscription_group_path(subscription_group) %></td>
    <td><%= link_to 'Destroy', subscription_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subscription group', new_subscription_group_path %>
